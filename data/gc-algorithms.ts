import type { GcAlgorithm } from '@/types';

export const gcAlgorithms: GcAlgorithm[] = [
  {
    id: 'serial',
    name: 'Serial GC',
    fullName: 'Serial Garbage Collector',
    introduced: 'Java 1.0',
    type: 'Single-threaded, Generational',
    description:
      'The simplest GC implementation. Uses a single thread for both minor (Young Gen) and major (Old Gen) collections. Freezes all application threads during collection — a classic Stop-The-World collector.',
    howItWorks: [
      'Uses mark-copy for Young Generation collection',
      'Uses mark-sweep-compact for Old Generation collection',
      'Single GC thread handles all collection work',
      'Application threads fully paused during collection',
      'Young Gen divided into Eden and two Survivor spaces (S0, S1)',
    ],
    pros: [
      'Simple and low memory overhead (no GC thread coordination)',
      'Predictable behavior — good for debugging',
      'Suitable for single-CPU machines or small heaps',
      'No floating garbage or concurrent overhead',
    ],
    cons: [
      'Long Stop-The-World pauses — unacceptable for interactive apps',
      'Does not scale with multiple CPU cores',
      'Cannot overlap GC work with application execution',
      'Not recommended for heap sizes over 200 MB',
    ],
    useCases: ['Command-line tools', 'Batch jobs', 'Small embedded JVMs', 'Single-core environments'],
    color: '#3B82F6',
    pauseType: 'Stop-The-World',
    throughput: 70,
    latency: 20,
    memoryOverhead: 10,
    flag: '-XX:+UseSerialGC',
  },
  {
    id: 'parallel',
    name: 'Parallel GC',
    fullName: 'Parallel / Throughput Garbage Collector',
    introduced: 'Java 1.4',
    type: 'Multi-threaded, Generational',
    description:
      'Uses multiple threads for GC work, parallelizing collection across CPU cores to maximize throughput. Like Serial GC it is still Stop-The-World, but the pause is shorter due to parallel execution. Default GC before Java 9.',
    howItWorks: [
      'Multiple GC threads run in parallel during Young Gen collection',
      'Old Gen uses Parallel Compacting collector (since Java 5)',
      'Number of GC threads defaults to number of CPUs',
      'Ergonomics auto-tune heap sizes to meet throughput / pause-time goals',
      'All application threads still pause during collection',
    ],
    pros: [
      'High throughput — maximizes CPU utilization for GC work',
      'Shorter pauses than Serial GC on multi-core hardware',
      'Automatic heap sizing and tuning via ergonomics',
      'Good for batch-processing and data-intensive workloads',
    ],
    cons: [
      'Still Stop-The-World — not suitable for latency-sensitive apps',
      'Pauses scale with heap size and live object count',
      'Compaction of Old Gen can cause long pauses with large heaps',
    ],
    useCases: [
      'Batch processing pipelines',
      'Scientific computation',
      'Throughput-focused server workloads',
      'Hadoop / Spark jobs',
    ],
    color: '#8B5CF6',
    pauseType: 'Stop-The-World',
    throughput: 90,
    latency: 45,
    memoryOverhead: 15,
    flag: '-XX:+UseParallelGC',
  },
  {
    id: 'g1',
    name: 'G1 GC',
    fullName: 'Garbage-First Garbage Collector',
    introduced: 'Java 7 (default since Java 9)',
    type: 'Concurrent, Generational, Region-based',
    description:
      'Divides the heap into equal-sized regions rather than fixed generational areas. Prioritizes collecting regions with the most garbage ("garbage-first"). Designed to achieve both high throughput and bounded pause times.',
    howItWorks: [
      'Heap split into ~2048 equal regions (typically 1–32 MB each)',
      'Regions dynamically assigned as Eden, Survivor, Old, or Humongous',
      'Concurrent Marking runs alongside application to find live objects',
      'Mixed Collection phases collect both Young and selected Old regions',
      'Pause targets respected via region selection — collects highest-garbage regions first',
      'Humongous objects (> 50% of region size) allocated in contiguous regions',
    ],
    pros: [
      'Predictable pause times via -XX:MaxGCPauseMillis target',
      'Concurrent marking overlaps with application execution',
      'Avoids full heap compaction in most cases',
      'Default GC for Java 9+ — well-tested at scale',
    ],
    cons: [
      'Higher memory overhead (~10–20%) for region bookkeeping',
      'Can still fall back to a full STW collection (Full GC) under pressure',
      'More complex tuning parameters than Parallel GC',
      'Concurrent phases can consume CPU resources',
    ],
    useCases: [
      'General-purpose server applications',
      'Web applications with mixed latency/throughput needs',
      'Heaps from 4 GB to 50 GB+',
      'Microservices with moderate latency requirements',
    ],
    color: '#10B981',
    pauseType: 'Mostly Concurrent',
    throughput: 85,
    latency: 75,
    memoryOverhead: 20,
    flag: '-XX:+UseG1GC',
  },
  {
    id: 'zgc',
    name: 'ZGC',
    fullName: 'Z Garbage Collector',
    introduced: 'Java 11 (production in Java 15)',
    type: 'Concurrent, Region-based, Colored Pointers',
    description:
      'A low-latency GC designed to keep pause times under 1 ms regardless of heap size, from a few hundred MB to multi-terabyte heaps. Uses colored pointer bits and load barriers to do almost all work concurrently.',
    howItWorks: [
      'Uses 64-bit colored pointers — metadata bits encode GC state of the pointed-to object',
      'Load barriers intercept pointer reads to remap references on the fly',
      'All marking, relocation, and remapping phases run concurrently with the application',
      'Only stop-the-world phases are very short root scanning pauses (<1 ms)',
      'Generational ZGC (Java 21+) adds generational support for even better throughput',
    ],
    pros: [
      'Sub-millisecond GC pauses regardless of heap size',
      'Scales from 8 MB to 16 TB heap',
      'Concurrent relocation eliminates compaction pauses',
      'Production-ready since Java 15; recommended for latency-critical systems',
    ],
    cons: [
      'Higher CPU overhead from load barriers and concurrent work',
      'Slightly lower throughput compared to Parallel GC',
      'Higher memory overhead due to multi-mapping and metadata',
      'Requires 64-bit JVM',
    ],
    useCases: [
      'Real-time trading systems',
      'Low-latency REST / gRPC APIs',
      'Gaming servers',
      'Any latency-sensitive service with a large heap',
    ],
    color: '#F59E0B',
    pauseType: 'Concurrent',
    throughput: 80,
    latency: 98,
    memoryOverhead: 25,
    flag: '-XX:+UseZGC',
  },
];
